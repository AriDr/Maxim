--I Would Recommend 4 Or More For The Teleport Time
getgenv().api = loadstring(game:HttpGet("https://raw.githubusercontent.com/Boxking776/kocmoc/main/api.lua"))()
getgenv().Enabled = false
local canTeleport = true
game:GetService("Workspace").Collectibles.ChildAdded:Connect(function(Tokens)
    if canTeleport == true and Enabled == true and Tokens:FindFirstChild("FrontDecal") then
        if (api.humanoidrootpart.Position - Tokens.Position)>=15 then
            canTeleport = false
            api:walkTo(Tokens.Position)
            task.wait(0.5)
            canTeleport = true
        else
            api:walkTo(game:GetService("Workspace").FlowerZones["Stump Field"].Position + vector3.new(0,1,0))
            task.wait(0.5)
            canTeleport = true
        end    
    end
end)
--REAL